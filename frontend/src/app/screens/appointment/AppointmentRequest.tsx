import { Typography } from "@mui/material";
import { Title } from "react-admin";

export function AppointmentRequest() {
  return (
    <div>
      <Title title="pages.AppointmentRequest" />
      <Typography>Appointment Request</Typography>
    </div>
  );
}
